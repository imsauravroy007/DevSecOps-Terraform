name: inputs-demo
on:   
  workflow_dispatch:
   inputs:
    environment:
      description: choose the environment to display
      required: true
      default: dev
jobs:
  inputs-demo:
   runs-on: ubuntu-latest
   steps:
     - name: deploy the app
       run: echo "deploy the app"
       
      
