name: Hello-world
on:
 push:
  branches:
    - main
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
    - name: hello-step
      run: echo "hello dev!"

  test:
   runs-on: ubuntu-latest
   needs: build
   steps:
    - name: hello-step1
      run: echo "hello test!"
